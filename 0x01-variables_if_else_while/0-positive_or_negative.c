#include <stdio.h>
#include <time.h>
/*
 * main - Print if number is positive, zero or negative
 *   
 * Return: Always (Success)
 *     
 */
int main(void)
{
		int n;
		srand(time(0));
		n = rand() - RAND_MAX / 2;
		return (0);
}
